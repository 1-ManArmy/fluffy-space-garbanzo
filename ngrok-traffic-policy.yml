on_http_request:
  - name: ForwardToOneLastAI
    actions:
      - type: forward-internal
        config:
          url: http://localhost:3000
          headers:
            host: localhost:3000
            x-forwarded-proto: https
            x-forwarded-host: humbly-tidy-coral.ngrok-free.app
